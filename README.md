Single web page with auto reload of `aircraft.json`.

With `aircraft.json` file being generated itself by
```
dump1090-mutability  --write-json-every 1 --write-json ./ --interactive   --net --metric
```
